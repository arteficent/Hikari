package com.example.android_client.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════
//  Washi-paper base tones  (shared across themes)
// ═══════════════════════════════════════════════════
val WashiCream      = Color(0xFFFAF3E8)   // warm aged-paper background
val WashiIvory      = Color(0xFFFFF8EF)   // lighter card surface
val WashiEdge       = Color(0xFFE8DFD0)   // subtle border / divider
val WashiText       = Color(0xFF2E2420)   // deep warm-brown text
val WashiTextSoft   = Color(0xFF5C4F45)   // secondary text
val GoldLeaf        = Color(0xFFCEA84C)   // glittering gold accent
val GoldLeafDim     = Color(0xFFB89535)   // pressed / darker gold
val GoldShimmer     = Color(0xFFE8D48C)   // shimmer highlight

// ── Wisteria (purple)  ────────────────────────────
val Wisteria         = Color(0xFF6B4C8A)   // primary purple
val WisteriaLight    = Color(0xFF9B7BBF)   // lighter lavender
val WisteriaPale     = Color(0xFFE8DCF4)   // very soft lavender container
val WisteriaMuted    = Color(0xFFC4ADDb)   // muted accent
val WisteriaDark     = Color(0xFF4A2D6B)   // deep wisteria

// ── Sakura (pink)  ────────────────────────────────
val SakuraPink       = Color(0xFFC47A8A)   // primary pink
val SakuraLight      = Color(0xFFE8A4B4)   // lighter rose
val SakuraPale       = Color(0xFFF5DDE3)   // very soft pink container
val SakuraMuted      = Color(0xFFD4949F)   // muted accent
val SakuraDark       = Color(0xFF8A4555)   // deep rose

// ── Golden leaf ───────────────────────────────────
val GoldPrimary      = Color(0xFFB08830)   // primary gold/amber
val GoldLight        = Color(0xFFD4AD5C)   // lighter gold
val GoldPale         = Color(0xFFF2E6C4)   // very soft gold container
val GoldAmber        = Color(0xFFC49B40)   // warm amber accent
val GoldDeep         = Color(0xFF7A5F20)   // deep amber

// ── Dark-mode palette  ────────────────────────────
val NightPaper       = Color(0xFF1A1715)   // warm dark background
val NightSurface     = Color(0xFF282320)   // card surface
val NightBorder      = Color(0xFF3D362F)   // border / outline
val NightText        = Color(0xFFEDE6DB)   // light warm text
val NightTextSoft    = Color(0xFFA89C8F)   // secondary text

// ── Celestial (space) palette ─────────────────────
val SpaceBlack       = Color(0xFF050508)   // deep space background (dark)
val SpaceLight       = Color(0xFFE8E4F0)   // light space background
val SpaceSurface     = Color(0xFF0C0C12)   // card surface in space
val SpaceBorder      = Color(0xFF1A1A28)   // faint nebula border
val SpaceText        = Color(0xFFE8E4F0)   // cool starlight text
val SpaceTextSoft    = Color(0xFF9090A8)   // secondary text
val StarWhite        = Color(0xFFF0EDE5)   // warm-white star
val StarBlue         = Color(0xFFAAC8E8)   // blue-white hot star
val CometTail        = Color(0xFFE8D48C)   // golden comet trail
val SunCorona        = Color(0xFFFFD96B)   // bright sun corona
val SunCore          = Color(0xFFFFECB3)   // sun centre glow
val CelestialGold    = Color(0xFFD4A843)   // primary gold accent
val CelestialGoldDim = Color(0xFFA88430)   // deeper gold